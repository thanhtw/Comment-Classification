BERT fine tuning
1. Training 80%
2. Detecting 20%

### Insatll
!pip install -U transformers datasets evaluate (ok)
!pip install scikit-learn (ok)
!pip install tensorboard (ok)
!pip install transformers (ok)
!pip install datasets pandas (ok)
!pip install evaluate (ok)


